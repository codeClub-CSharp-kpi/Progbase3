<Window x:Class="MoiveHubSystem.Views.ActorsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MoiveHubSystem.Views"
        mc:Ignorable="d"
        Title="Actor Window" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>


        <DockPanel VerticalAlignment="Center" Grid.RowSpan="3" Grid.Column="2" >
            
            <Label Content="Name:" DockPanel.Dock="Top" />
            <TextBox DockPanel.Dock="Top" />
            <Label DockPanel.Dock="Top" Content="Patronimic:" />
            <TextBox DockPanel.Dock="Top" />
            <Label DockPanel.Dock="Top" Content="Surname:" />
            <TextBox DockPanel.Dock="Top" />
            <Label DockPanel.Dock="Top" Content="Bio:" />
            <TextBlock DockPanel.Dock="Top" Text=""/>
                

            
        </DockPanel>



        <ListBox x:Name="actorsList" ItemsSource="{Binding Actors}"/>

        <DockPanel Grid.Column="1" Grid.RowSpan="3" >
            <Image />
        </DockPanel>

        <WrapPanel Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="3" Margin="10">
            <Button Margin="5 0" Content="Previous" />
            <Button Margin="5 0" Content="Next"/>
        </WrapPanel>
        <ScrollViewer Margin="10" Grid.Column="0" Grid.Row="1" Width="auto">
            <StackPanel>
                <Button Margin="5" Content="Add"/>
                <Button Margin="5" Content="Delete"/>
                <Button Margin="5" Content="Save changes"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
